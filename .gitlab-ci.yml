stages:
  - test
  - build

test:
  stage: test
  image: python:3.10-slim
  script:
    - pip install pytest
    - pytest --version
    - echo "Tests passed!"

build:
  stage: build
  image: alpine:latest
  script:
    - echo "Building application..."
    - echo "Build complete!"